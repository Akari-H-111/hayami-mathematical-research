v0.59 Normalized-Source Generation Theorem

INPUT IS NO LONGER N.

Marked deformation-equation package:
  source space F
  distinguished relation seed map R0:W_rel->F
  source-side constraint/syzygy propagators C_i in End(F)

GENERATION:
  N^(0)=im R0
  N^(r+1)=N^(r)+sum_i C_i N^(r)

STABLE MODULE:
  N_eq = stable value
       = k<C_1,...,C_s> . im R0

UNIVERSAL PROPERTY:
  N_eq is the unique smallest source subspace containing im R0 and
  invariant under all C_i.

FINITE CLOSURE:
  stabilizes after <= dim F strict rank increases.

FUNCTORIALITY:
If phi_F R0 = R0' phi_W and phi_F C_i = C_i' phi_F, then
  phi_F(N_eq) subset N_eq'.
For an isomorphism of relation-generation packages:
  phi_F(N_eq)=N_eq'.

BOUNDARY PRESERVATION:
If seed lies in a canonical boundary/constraint subspace F_bc and all
C_i preserve F_bc, then N_eq subset F_bc.

CUBIC:
  seed beta=xi cup xi=-dE
  resonant source line closes on itself
  N_eq=C beta
without referring to chosen contraction.

LINK TO v0.58:
  Gamma_eq = nu_eq D Lambda
  K ~= ker Gamma_eq / ker Lambda
  A|K induced from T quotient.

LINK TO v0.57:
  extracted A|K gives exact common characteristic/minimal spectral
  divisor across normalized tilt orbit.

EXACT TEST:
  closure ranks 2 -> 4 -> 5 -> 5
  functorial under nontrivial basis change
  generated N_eq dim 5
  forced char/min polynomial:
    (S+2)^2 (S-3)^2 (S-5)

SCOPE:
  marked-deformation-equation invariant, not ordinary unmarked
  quasi-isomorphism invariant, because exact boundary seeds may be
  contractibly cancelled.

NEXT:
Syzygy-to-Constraint Propagator Theorem:
derive the operator algebra generated by the C_i directly from the
syzygy complex of the deformation presentation.
